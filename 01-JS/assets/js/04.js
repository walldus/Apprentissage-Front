                            /* ----------------------------------------------
                            |               ~  LES OPERATEURS  ~  😅        |
                            ----------------------------------------------- */


    // ~~ L'ADDITION ~~ //

    // -- Déclaration de plusieurs variables à la suite
    var nb1,nb2,resultat;

    nb1 = 10;
    nb2 = 5;

    // -- Addition de nb1+nb2 avec l'opérateur "+"
    resultat = nb1 + nb2;

    // -- Affichage du résultat dans la console
    console.log( "L'addition de nb1 + nb2 est égale à : " + resultat );


// ~~ LA SOUSTRACTION ~~ //

    // -- La soustraction de nb1+nb2 avec l'opérateur "-"
    resultat = nb1 - nb2;

    // -- Affichage du résultat dans la console
    console.log( "L'addition de nb1 - nb2 est égale à : " + resultat );


// ~~ LA MULTIPLICATION ~~ //

    // -- La multiplicarion de nb1 et nb2 avec l'opérateur "*"
    resultat = nb1 * nb2;

    // -- Affichage du résultat dans la console
    console.log( "L'addition de nb1 * nb2 est égale à : " + resultat );


// ~~ LA DIVISION ~~ //

    // -- La multiplicarion de nb1 et nb2 avec l'opérateur "/"
    resultat = nb1 / nb2;

    // -- Affichage du résultat dans la console
    console.log( "L'addition de nb1 / nb2 est égale à : " + resultat );


// ~~ LE MODULO ~~ //

    // -- La multiplicarion de nb1 et nb2 avec l'opérateur "%"
    nb1 = 11;
    resultat = nb1 % nb2;

    // -- Affichage du résultat dans la console
    console.log( "L'addition de nb1 % nb2 est égale à : " + resultat );


                            /* -------------------------------------------------
                            |       ~  LES ECRITURES SIMPLIFIEES  ~  😅        |
                            -------------------------------------------------- */

nb1 = 15;
nb1 = nb1 + 5;
console.log(nb1);

nb1 += 5; // Ce qui equivaut a écrire nb1 = nb1 + 5;
console.log(nb1);

// -- Je peux procéder de la même maniére pour tous les autres opérateurs arithmétiques : " + ", " - ", " * ", " / ", " % "
